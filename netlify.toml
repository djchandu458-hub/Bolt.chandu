[build]
  command = "pnpm install && pnpm run build"
  publish = "build"

[context.production.environment]
  # Do NOT put your real key here. Add it in Netlify UI as described below.
  GEMINI_API_KEY = ""
